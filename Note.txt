Babel can be configured via .babelrc but I prefer to use package.json

Four ways to Create React Components.
- createClass
- ES Class 
- Function
- Arrow Function

Immutability:
To change state, return a new object

"start": "run-p start:dev start:api" // This command allows us to run multiple npm scripts at the same time.